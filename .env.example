APP_MODE=api
PORT=3000
OPENAI_API_KEY=sk-...

# MongoDB (mesmo banco que tax-agent-hub, collections com prefixo tvo-)
MONGODB_URI=mongodb://localhost:27017/tax-agent-hub

# URL da API tax-agent-hub (prioridade sobre TAX_AGENT_HUB_PATH)
# Quando configurado, schemaLookupTool e vectorStoreMapping consomem via API
TAX_AGENT_HUB_URL=http://localhost:3001

# Caminho para o diretório tax-agent-hub (fallback quando TAX_AGENT_HUB_URL não configurado)
TAX_AGENT_HUB_PATH=
